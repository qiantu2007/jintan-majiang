# 本次改动：花牌改成手动补（v1.17.1 → v1.18.0）

> 这份是交接说明，给接手的人看的。看完可以删。
> 改动日期 2026-09-01。改之前项目没有 git，所以没有 diff，只能逐条列。

## 需求

原话：「我希望摸到的花可以自己打出去再摸下一张牌，这样更加真实，有体验感」。

确认过是**手动**（亲手点一下才补），范围**单机 + 联机都改**。

## 行为对比

| | 改之前 | 改之后 |
|---|---|---|
| 摸到花 | 瞬间自动亮出 + 从牌尾补一张，看不见过程 | 牌局停住，花牌留在手里等你点 |
| 怎么补 | 不用管 | 点那张花，或点弹出来的「补花 X」按钮 |
| 连续摸到花 | 一次性全补完 | 一张一张停，点几次补几次 |
| 补花期间 | —— | 出牌 / 胡 / 杠 按钮全部收起 |
| 电脑 / 掉线托管 | 自动补 | **仍然自动补**（不变） |
| 开局发牌的花 | 自动补 | **仍然自动补**（没改，见「未做」） |

---

## 一、客户端 `index.html`

单文件，所有代码在唯一那个内联 `<script>` 里（IIFE）。行号是改完之后的。

### 新增函数（4 个）

| 行 | 函数 | 作用 |
|---|---|---|
| 5061 | `waitHumanFlower(tile)` | 单机：返回 Promise，弹「补花」按钮，等人点 |
| 5078 | `startFlowerTimer(resolveFlower)` | 单机：倒计时，超时替她补掉（复用 `settings.timer`） |
| 5092 | `doHumanFlower()` | 单机：兑现那个 Promise。开头有 `typeof !== "function"` 守卫，联机不会误入 |
| 3804 | `netAskFlower()` | 联机：收到 `needFlower` 后弹按钮 |
| 3819 | `netSendFlower()` | 联机：发 `{t:"act", a:"flower"}` 给服务端 |

### 改动的地方

| 行 | 位置 | 改了什么 |
|---|---|---|
| 1558 | `VERSION` | `"1.17.1"` → `"1.18.0"` |
| 1559 | `CHANGELOG` | 顶部插入 1.18.0 条目，5 条 |
| 1422 | 帮助页 `<li>` | 「摸到花牌会自动亮出…」→ 改成手动的说法 |
| 1794 | `ui` 对象 | 新增 `flowerWait: null, flowerId: null` |
| 5101 | `replaceFlowers(p)` | 加 `byHand = !game.online && p === game.players[0]`；`byHand` 时先 `game.phase="flower"` 再 `await waitHumanFlower()`，醒来后**重新定位那张花**（等待期间手牌可能被 `sortHand` 重排）；停顿 `byHand ? 240 : 380` ms |
| 4812 | `#hand` click | 最前面加分支：`ui.flowerWait` 时点中那张花就补，点别的提示「先把花补掉」 |
| 4919 | `#claim` click | 加 `data-act === "flower"` 分支，联机走 `netSendFlower()`，单机走 `doHumanFlower()` |
| 4887 | `refreshActBar()` | 加 `if (ui.flowerWait) { setAct(""); return; }` |
| 4008 | `netRefreshBar()` | 加 `if (ui.flowerWait \|\| game.phase === "flower") { setAct(""); return; }` |
| 3800 | `netDrew(m)` | `if (m.needFlower) { netAskFlower(); return; }` |
| 3780 | `netTurn(m)` | **关键修复**：`if (game.phase === "flower" && game.current === 0) { netAskFlower(); return; }` |
| 3764/3768 | `netResume(m)` | phase 按 `m.needFlower` 定；是的话弹按钮并提示「接上了，先把花补掉」 |
| 4027 | `netPlaySelected()` | toast 文案改成「花牌点「补花」亮出去，不能当废牌打」 |
| 4960 | `onHumanPlay()` | 同上（**这处是补核对时才发现漏改的**，两处变量名不同 `t.key` / `tile.key`，第一次没一起替换掉） |
| 5975 | `startRound()` | 清 `ui.flowerWait` / `ui.flowerId` |
| 6374 | `#btn-undo` | 悔牌时兑现并清掉挂起的补花等待，否则会卡住 |

### `netTurn` 那处修复为什么关键

服务端消息顺序是 `drew` → `drewn` → `turn`。`netTurn` 里原本有 `setClaim("")`，
会把 `netDrew` 刚弹出来的「补花」按钮**冲掉**，玩家就没东西可点了。
所以要在 `netTurn` 里提前拦下来重新弹一次。

---

## 二、服务端 `_服务端/worker.js`

### 拆分与新增

原来的 `autoFlower(i)` 是个「一次性补到底」的循环。拆成：

| 行 | 方法 | 作用 |
|---|---|---|
| 388 | `oneFlower(i)` | **只补一张**。返回 `{flower, tile}`；没花了返回 `null`，牌墙空了 `tile` 为 `null` |
| 400 | `autoFlower(i)` | 循环调 `oneFlower`，语义和原来一致（开局发牌、电脑、托管走这条） |
| 405 | `hasFlower(i)` | 手里还有没有花 |
| 412 | `settleFlower(i)` | 摸完牌后的决策：没花→false；`unattended(i)`（电脑/掉线）→自动补完返回 false；否则置 `phase="flower"` 返回 true |

### 改动的地方

| 行 | 位置 | 改了什么 |
|---|---|---|
| 728 | `drawBack(i)` | `autoFlower` → `settleFlower`，phase 按需要设，`drew` 消息带上 `needFlower` |
| 746 | `nextTurn(from)` | 同上 |
| 565 | `onAct` 新增 `a === "flower"` | 校验 `current===who && !claim && phase==="flower"`；补一张；还有花就继续停，没了转 `discard`；复用 `drew`/`drewn` 消息下发 |
| 587 | `onAct` 的 `discard` 分支 | **兼容旧客户端**：欠着花却收到出牌请求时，先 `autoFlower` 补完再处理；要打的那张若正好被当花补走了就只补花 |
| 539 | `aiFallback()` | 人跑了还欠花时先 `autoFlower` 补完，否则会把那张花当废牌打出去 |
| 802 | `pushResume(seat)` | 加 `needFlower: g.phase === "flower" && g.current === seat` |

### 为什么加「兼容旧客户端」那段

新服务端 + 旧客户端时，服务端会停在补花态等一条旧客户端永远不会发的消息，
牌局要卡到 30 秒兜底才恢复。加了这段之后**两边谁先部署都不会卡**。
新客户端在补花期间根本不给出牌按钮，走不到那个分支。

---

## 三、版本与部署文件

| 文件 | 改动 |
|---|---|
| `version.json`（根 + `_上传这个文件夹`） | `1.17.1` → `1.18.0` |
| `sw.js`（根 + `_上传这个文件夹`） | `CACHE = "jintan-mj-v171"` → `"jintan-mj-v180"` |
| `_上传这个文件夹/index.html` | 从根目录同步（改之前它还是旧版，两份内容不一致） |

`sw.js` 的缓存名必须换，否则老用户刷新拿到的还是缓存里的旧页面。

**没有动过的文件**：`wrangler.toml`（两个）、`manifest.webmanifest`、图标、
`_牌面素材/`、`index.grok-v1.backup.html`、各 `_测试_*.mjs`、两份 `部署步骤.md`。

---

## 四、验证做过什么

- `node --check` 过了 `worker.js`；用脚本把 `index.html` 的内联 script 抽出来单独 `node --check`，也过了。
- 起本地 http 服务，用一份**带调试钩子的临时副本**（把 `game`/`ui` 暴露到 `window.__dbg`，
  真文件没动，测完已删）把牌墙做成确定顺序来测：

| 场景 | 结果 |
|---|---|
| 摸到单张花 | 停在 `phase:"flower"`，弹「补花 梅」，出牌按钮消失 |
| 点按钮补 | 手牌 14→14（f5 移出、p3 补入），花区 +f5，牌墙 86→85（确认是**牌尾**） |
| 连续两张花 | 点手牌 f4 → 补进来是 f8 → 再次停住 → 点按钮 → 花区 [f5,f4,f8]，回 `discard` |
| 两种点法 | 点手牌那张花、点「补花」按钮，都通 |
| AI 摸到花 | 自动补掉，牌局不停顿 |
| 控制台 | 0 错误 |

**联机路径没做端到端实测**（需要部署服务端 + 两个客户端连上来）。
代码逻辑和断线重连分支是照着现有 `claim`/`discard` 的模式写的，但没跑过真实链路。

---

## 五、未做 / 已知问题

1. **开局发牌的补花仍然是自动的**。真牌桌上开局那几张也是一张张补，
   但那意味着开打前先点 0–8 次，所以先没动。要改的话改 `worker.js:369` 那句
   `for (let i = 0; i < 4; i++) this.autoFlower(i);` 和单机的 `dealTiles` 之后那段。
2. **服务端没有「一人独得 8 张花直接封顶结束」的判定**。
   单机 `replaceFlowers` 里有 `p.flowers.length >= 8 → endEightFlowers`，
   `worker.js` 里搜不到任何对应逻辑。**这是本次改动之前就存在的**，不是这次弄坏的。
   → 复核结论：**保留现状**。一个人独占全部 8 张花的概率约万分之六，
   补上要新增服务端结束分支 + 客户端 `kind:"eight"` 的结算路径，
   新代码的风险大于这个场景的收益。单机和联机在这一点上行为不同，记在这里。
3. ~~联机端到端未实测~~ → **已补测，见下面第七节。**

---

## 七、复核补测（2026-09-01，第二轮）

### 找到并修掉的两个问题

**1. 补花状态没清干净会把整局锁死（严重）**

`netResume` 在 `m.needFlower` 为假时不清 `ui.flowerWait`。触发路径：

> 欠着花没点 → 手机锁屏掉线 → 服务端 30 秒兜底替他补花并出牌
> → 他连回来，`needFlower` 已经是 false → 但 `ui.flowerWait` 还是 true
> → `netRefreshBar()` 第一行就 `return` → **出牌按钮再也不出现，这一局废了**

`netStartGame`（下一盘）和 `netOver`（结算）同样没清。

修法：新增 `clearFlowerWait()`，在 `netResume` / `netStartGame` / `netOver` /
`netClose` 四处调用。挂起的 Promise 也一并兑现，避免单机路径吊着。

A/B 验证（`_服务端/_测试_补花死结.mjs`，把相关几行抽出来单独跑）：

| | 补花时操作栏 | 重连后操作栏 | 还能出牌 |
|---|---|---|---|
| 不带修复 | 空 | 空 | **不能，卡死** |
| 带修复 | 空 | `<button>打出</button>` | 能 |

**2. 补花提示语被切掉半截**

大牌面＋特大字时牌桌只剩 180px 高，中间信息框 119px 塞进 88px 的 `.mid` 区，
**下边超出牌桌 11px**，「亮出来」三个字被手牌区盖住。

修法：提示语从「摸到「梅」，点「补花」亮出来」缩成「摸到「梅」，点补花」
（按钮上本来就写着「补花 梅」，不必重复）；另外给 `body[data-tsize="big"] .msg`
放宽 max-width、压紧行高。改后提示回到一行，信息框超出量 −1px（不再超）。

### 补做的联机端到端实测

新增 `_服务端/_测试_手动补花.mjs`，对着本地 `wrangler dev` 跑真实协议：

| 场景 | 结果 |
|---|---|
| 开局发牌的花仍自动补 | ✓ 手里没有花 |
| 摸到花服务端停住 | ✓ `phase=flower`，`drew` 带 `needFlower` |
| **欠着花的那一刻断线** | ✓ 手牌张数没变 |
| 重连后 `resume` 带 `needFlower` | ✓ 能接着补 |
| 补花：亮一张补一张 | ✓ 手牌 14→14，牌墙 −1（牌尾） |
| 打到结束全程守恒 | ✓ 牌张没有凭空多出来 |
| 走到流局 | ✓ 正常结束 |

单机也用发布版文件（不是调试副本）跑了整局：手动补花正常、144 张全程守恒、
四个面板正常、零报错、自检通过。上一轮的断线重连修复复测也没被破坏。

### 版本

`1.18.0` → `1.18.1`，`sw.js` 缓存名 `v180` → `v181`，
根目录与 `_上传这个文件夹` 已逐个文件校验 MD5 一致。

---

## 六、还需要做的

1. **部署服务端**（联机才需要，两条命令顺序不能反）：
   - `_服务端` 里 `npx wrangler deploy`
   - `_服务端Pages` 里 `npx wrangler pages deploy`
2. **上传客户端**：`_上传这个文件夹` 已经同步好，传到平时放客户端的地方。
